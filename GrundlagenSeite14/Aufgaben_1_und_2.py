def ausgeben(wert, waehrung):
 print(wert, waehrung)

ausgeben("Betrag: ", 5, "EURO")

# Aufgabe 1

# a) 5 EURO als ausgabe. 5 als int, EURO als str
# b) wie a nur umgekehrte automatische vergabe des Datentyps. Von Namen der variable nicht sinnig, aber das weiß das
# Programm nicht
# c) TypeError: ausgeben() missing 1 required positional argument: 'waehrung'
#     Diese Fehlermeldung gib an, dass die parameteranzahl genau 2 betragen muss, sonst weigert sich der compiler dies
#     auszugeben
# d) TypeError: ausgeben() takes 2 positional arguments but 3 were given
#     Dieser Fehlermeldung gibt an, dass die parameteranzahl genau 2 betragen muss, und ein Parameter zu viel ist um es
#     auszuführen


# Aufgabe 2
# - einfache knapper code zur wiederholten Ausgabe/Nutzung von ganzen codeblöcken
# - strukturierterer Code ohne Redundanz => spart Platz und Zeit

